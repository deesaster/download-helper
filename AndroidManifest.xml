<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.leetr"
          android:versionCode="1"
          android:versionName="1.0">
    <!--
    <application android:label="LeetrBase" android:icon="@drawable/icon" android:debuggable="false"
                 android:theme="@style/Theme.LeetrBase" android:name="com.leetr.core.LeetrApplication">

        <activity android:name="com.leetr.core.LoaderActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.leetr.activity.LeetrActivity" android:label="@string/app_name"/>
        <activity android:name="com.leetr.activity.LeetrDashboardActivityivity" android:label="@string/app_name"/>
        <activity android:name="leetr.base.activities.TabActivity" android:label="@string/app_name"/>
        <activity android:name="leetr.base.activities.CrashHelpActivity" android:label="@string/app_name"/>
    </application>
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- location services-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Needed for caching thumbnails -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- Needed for supporting HDPI devices, without this line only drawable-mdpi will be used -->
    <supports-screens android:anyDensity="true"/>
</manifest> 
